# iOS · GrokBot Body

Xcode 打开 `GrokBotBody.xcodeproj`，Team 用你的 Apple ID，装到真机。第一次允许相机、麦克风、相册。

绑 Bot、门铃、听和说，见仓库根目录 `README.md` 和 `docs/binding.md`。

绑定页「云」填自己部署的 Worker。本地调试：`cd ../cloud && npm run dev`，地址填 `http://<这台 Mac 的局域网 IP>:8787`。
