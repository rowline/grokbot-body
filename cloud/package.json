{
  "name": "grokbot-body-cloud",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "test": "vitest run",
    "check": "wrangler types && tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "latest",
    "@cloudflare/workers-types": "latest",
    "@types/node": "^26.4.0",
    "typescript": "^5.9.2",
    "vitest": "^4.1.0",
    "wrangler": "latest"
  }
}
